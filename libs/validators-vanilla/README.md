# validators-vanilla

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build validators-vanilla` to build the library.

## Running unit tests

Run `nx test validators-vanilla` to execute the unit tests via [Vitest](https://vitest.dev/).
