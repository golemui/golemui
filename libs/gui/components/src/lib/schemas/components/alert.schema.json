{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://golemui.com/schemas/components/alert.schema.json",
  "title": "Alert Widget",
  "type": "object",
  "$defs": {
    "localizableText": {
      "$ref": "../common.schema.json#/definitions/localizable"
    },
    "alertLevel": {
      "enum": ["default", "info", "success", "warning", "error"]
    }
  },
  "allOf": [
    {
      "$ref": "../common.schema.json#/definitions/baseWidget"
    }
  ],
  "properties": {
    "kind": {
      "const": "display"
    },
    "type": {
      "const": "alert"
    },
    "props": {
      "type": "object",
      "properties": {
        "text": {
          "$ref": "#/$defs/localizableText"
        },
        "level": {
          "$ref": "#/$defs/alertLevel"
        }
      },
      "patternProperties": {
        "^text\\.[^.]+$": {
          "$ref": "#/$defs/localizableText"
        },
        "^level\\.[^.]+$": {
          "$ref": "#/$defs/alertLevel"
        }
      },
      "required": ["text"],
      "additionalProperties": false
    }
  },
  "required": ["kind", "type", "props"]
}
