/**
 * The published form schema URL emitted as `$schema` in every example and
 * generated form definition. It is kept resolving by the alias file
 * `libs/schemas/site/form.schema.json`, which forwards to the gui form
 * envelope. A spec asserts this constant matches that alias `$id`.
 */
export const FORM_SCHEMA_URL = 'https://golemui.com/schemas/form.schema.json';
