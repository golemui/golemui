{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://golemui.com/schemas/form.schema.json",
  "$comment": "Legacy alias. Keeps the original schema URL working by forwarding to the gui implementation tree. A website build input only: the ref target exists in the published site tree, never inside an npm package, so this file is not shipped to npm.",
  "$ref": "./gui/form.schema.json"
}
