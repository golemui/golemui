{
  "name": "analog-playground",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/analog-playground/src",
  "tags": ["scope:app", "type:app"],
  "// targets": "to see all targets run: nx show project analog-playground --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production", { "externalDependencies": ["@analogjs/platform"] }],
      "outputs": ["{workspaceRoot}/dist/apps/analog-playground"],
      "options": {
        "cwd": "apps/analog-playground",
        "command": "npx vite build"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "cwd": "apps/analog-playground",
        "command": "npx vite dev"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "PORT=4000 node dist/apps/analog-playground/analog/server/index.mjs"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "production",
        "^production",
        { "externalDependencies": ["@analogjs/platform", "typescript"] }
      ],
      "options": {
        "cwd": "apps/analog-playground",
        "command": "npx tsc -p tsconfig.app.json --noEmit"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
