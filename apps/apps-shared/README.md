# apps-shared

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build apps-shared` to build the library.

## Running unit tests

Run `nx test apps-shared` to execute the unit tests via [Vitest](https://vitest.dev/).
