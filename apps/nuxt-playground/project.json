{
  "name": "nuxt-playground",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/nuxt-playground/app",
  "tags": ["scope:app", "type:app"],
  "// targets": "to see all targets run: nx show project nuxt-playground --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production", { "externalDependencies": ["nuxt"] }],
      "outputs": ["{workspaceRoot}/dist/apps/nuxt-playground"],
      "options": {
        "cwd": "apps/nuxt-playground",
        "command": "npx nuxt build"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "cwd": "apps/nuxt-playground",
        "command": "npx nuxt dev"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "PORT=3700 node dist/apps/nuxt-playground/server/index.mjs"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "production",
        "^production",
        { "externalDependencies": ["nuxt", "vue-tsc", "typescript"] }
      ],
      "options": {
        "cwd": "apps/nuxt-playground",
        "command": "npx nuxt typecheck"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
