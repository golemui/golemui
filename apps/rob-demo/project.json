{
  "name": "rob-demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/rob-demo/src",
  "projectType": "application",
  "tags": ["scope:app", "type:app"],
  "// targets": "to see all targets run: nx show project rob-demo --web",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite",
        "cwd": "apps/rob-demo"
      }
    }
  }
}
