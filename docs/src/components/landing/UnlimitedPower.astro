---
import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import AngularForm from '../../../public/template/code-samples/angular/my-golemui-form.component.ts?raw';
import ReactForm from '../../../public/template/code-samples/react/MyGolemUIForm.tsx?raw';
import LitForm from '../../../public/template/code-samples/lit/my-golemui-form.element.ts?raw';
---

<section class="custom-power">
  <div class="container">
    <div class="content">
      <span class="section-label">Platform</span>
      <h2>
        Unlimited <span class="gradient-text">Power</span>.<br />Made <span class="gradient-text"
          >Easy</span
        >.
      </h2>
      <p>GolemUI is the easiest and fastest library to build dynamic forms.</p>
    </div>
  </div>
  <div class="container-demo">
    <div class="home-examples">
      <Tabs>
        <TabItem label="Angular">
          <Code code={AngularForm} lang="ts" />
        </TabItem>
        <TabItem label="React">
          <Code code={ReactForm} lang="tsx" />
        </TabItem>
        <TabItem label="Lit">
          <Code code={LitForm} lang="ts" />
        </TabItem>
      </Tabs>
    </div>
    <div class="iframe-example">
      <iframe src="tiny/index.html" title="Custom widgets" width="100%" height="596px"></iframe>
    </div>
  </div>
</section>

<style lang="scss">
  iframe {
    border: none;
  }

  .iframe-example {
    width: 100%;
    height: 250px;

    @media (min-width: 50rem) {
      width: 50%;
      height: 596px;
    }
  }

  .home-examples {
    width: 100%;
    background-color: transparent;

    @media (min-width: 50rem) {
      width: 50%;
    }
  }

  .custom-power {
    padding: 4rem 1rem;
    background: var(--bg-surface);
    overflow: hidden;

    @media (min-width: 361px) {
      padding: 4rem 1.5rem;
    }

    @media (min-width: 768px) {
      padding: 4rem 2rem;
    }
  }

  .container-demo {
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border-radius: 16px;
    max-width: 1200px;
    margin: 0 auto;
    border: 1px solid var(--border-color);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.06);

    @media (min-width: 50rem) {
      flex-direction: row;
    }
  }

  html.dark .container-demo {
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
  }

  .content {
    h2 {
      font-size: 2.5rem;
      font-weight: 800;
      line-height: 1.08;
      margin: 1.25rem 0;
      letter-spacing: -0.03em;

      @media (min-width: 768px) {
        font-size: 4.5rem;
      }
    }

    p {
      font-size: 1.125rem;
      color: var(--text-muted);
      margin-bottom: 3rem;
      line-height: 1.7;

      @media (min-width: 768px) {
        font-size: 1.25rem;
      }
    }
  }

  iframe {
    border: 0;
  }

  html.dark {
    .home-examples {
      background-color: var(--ec-codeBg);
    }
  }
</style>
