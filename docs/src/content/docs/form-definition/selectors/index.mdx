---
title: Selectors
description: The behavior layer — decorate widgets by type, tag, or uid without touching the form structure.
sidebar:
  order: 0
---

import IntegrationTabs from '../../../../components/IntegrationTabs.astro';

Selectors are the **behavior layer** of the Form Definition API. While `gui.inputs.*`, `gui.layouts.*`, etc. describe the form's **structure**, `gui.selectors.*` describes how those widgets should look and behave — by **type**, by **tag**, or by **uid** — without you having to weave that behavior into every shortcut call.

A selector is built by chaining methods off the `gui.selectors` root, then matching a target type and supplying a config:

```ts
import { gui } from '@golemui/gui-shared';

const formSelectors = [
  // Suppress auto-labels on every input
  gui.selectors.inputs({ suppressAutomaticLabels: true }),

  // For inputs tagged 'identity', also suppress auto-placeholders
  gui.selectors.tag('identity').inputs({ suppressAutomaticPlaceholders: true }),

  // Hide a specific dropdown by uid in a particular state
  gui.selectors.state('loaded').dropdownByUid('country', { disabled: false }),
];
```

Pass the array as `formSelectors` inside the `config` object:

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import { GuiForm } from '@golemui/gui-react';

    const config = { formDef, formSelectors };
    // <GuiForm config={config} />
    ```

  </Fragment>
  <Fragment slot="angular">
    ```ts
    import { Component } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { FormComponent } from '@golemui/gui-angular';

    @Component({
      imports: [CommonModule, FormComponent],
      selector: 'app-my-form',
      template: `<gui-form [config]="config"></gui-form>`,
    })
    export class MyForm {
      protected config = { formDef, formSelectors };
    }
    ```

  </Fragment>
  <Fragment slot="lit">
    ```ts
    import { LitElement, html } from 'lit';
    import { customElement } from 'lit/decorators.js';
    import '@golemui/gui-lit';

    @customElement('my-form')
    export class MyForm extends LitElement {
      config = { formDef, formSelectors };

      override createRenderRoot() { return this; }

      override render() {
        return html`<gui-form .config=${this.config}></gui-form>`;
      }
    }
    ```

  </Fragment>
  <Fragment slot="vue">
    ```vue
    <script setup lang="ts">
    import { GuiForm } from '@golemui/gui-vue';

    const config = { formDef, formSelectors };
    </script>

    <template>
      <GuiForm :config="config" />
    </template>
    ```

  </Fragment>
</IntegrationTabs>

## Read on

- [Type selectors](/form-definition/selectors/type-selectors/) — `inputs`, `dropdowns`, `actions`, etc.
- [Chaining](/form-definition/selectors/chaining/) — composing scope methods.
- [Scope operators](/form-definition/selectors/scope-operators/) — `tag`, `state`.
- [Multi-value scope operators](/form-definition/selectors/multi-value-scopes/) — `tagsAnd`, `tagsOr`.
- [Sensible defaults](/form-definition/selectors/sensible-defaults/) — what the engine auto-fills for you.
- [Precedence](/form-definition/selectors/precedence/) — the merge order between defaults, selectors, and shortcut props.

## See also

- [Form Definition Overview](/form-definition/overview/) — the full `gui.*` namespace.
