---
title: Tabs
description: A Layout widget for organizing content into separate views accessible via a navigation bar
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import tabsJson from '../../../../../public/template/widgets/tabs/tabs.json?raw';
import tabsDx from '../../../../../public/template/widgets-dx/tabs/tabs.ts?raw';
import tabsDefaultOpenJson from '../../../../../public/template/widgets/tabs/tabs-default-open.json?raw';
import tabsDefaultOpenDx from '../../../../../public/template/widgets-dx/tabs/tabs-default-open.ts?raw';
import tabsRenderModeJson from '../../../../../public/template/widgets/tabs/tabs-render-mode.json?raw';
import tabsRenderModeDx from '../../../../../public/template/widgets-dx/tabs/tabs-render-mode.ts?raw';
import tabsAnatomyHtml from '../../../../../public/template/widgets/tabs/tabs-anatomy.html?raw';
export const configJson = 'tabs.json';
export const configDx = 'tabs.ts';
export const anatomy = 'tabs-anatomy.html';

Tabs widgets are [Layout Fields](/widgets-reference/layout-fields/overview) that allow users to switch between different content areas within the same context. They are highly effective for breaking down large forms into logical, manageable pieces.

<DslCode dxCode={tabsDx} jsonCode={tabsJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/tabs/tabs.json"
/>

## Props

Use these `props` to customize your Tabs widget.

| prop          | type   | description                                                                                                                                                         |
| ------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tabs`        | array  | **Required**. Array of tab definitions: `{ label: string, uid: string }`. Each `uid` must match the `uid` of one of the children.                                   |
| `defaultOpen` | string | The `uid` of the tab that should be active on initial render. Defaults to the first tab                                                                             |
| `renderMode`  | string | `'all'` keeps every tab panel mounted in the DOM and toggles visibility; `'activeOnly'` mounts the active tab's children only while it is open. Defaults to `'all'` |

### Default Open

Use `defaultOpen` to select which tab is active on first render. Set it to the `uid` of any tab — here, the second tab is selected on load.

<DslCode dxCode={tabsDefaultOpenDx} jsonCode={tabsDefaultOpenJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/tabs/tabs-default-open.json"
/>

### Render Mode

Use `renderMode` to control when tab children are mounted. The default `'all'` keeps every panel in the DOM and hides inactive ones, which preserves child state and form values as the user switches tabs. Use `'activeOnly'` to mount a tab's children only while it is active — useful when panels contain expensive widgets, side effects on mount, or media you don't want loaded up front. Inspect the rendered DOM as you switch tabs to see the difference.

<DslCode dxCode={tabsRenderModeDx} jsonCode={tabsRenderModeJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/tabs/tabs-render-mode.json"
/>

## Styling

Tabs can be styled as explained in the [Styling Guide](/styling/overview/).

### CSS Variables

Following you will find a list with the CSS Variables used to style the tabs.

| CSS Variable           | Description                                 |
| ---------------------- | ------------------------------------------- |
| `--gui-bg-default`     | Background color for tab buttons and panels |
| `--gui-border-default` | Border color for tab buttons and panels     |
| `--gui-text-default`   | Text color for labels                       |
| `--gui-space-3`        | Padding inside tab buttons and panels       |
| `--gui-radius-md`      | Border radius for tab buttons and panels    |

### Anatomy

This is the anatomy of the Tabs Widget in case you want to use your CSS styles.

<Code code={tabsAnatomyHtml} lang="html" title={anatomy} />
