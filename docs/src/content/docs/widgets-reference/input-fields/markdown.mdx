---
title: Markdown
description: A Markdown editor widget with a formatting toolbar and live preview
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import IntegrationTabs from '../../../../../src/components/IntegrationTabs.astro';
import markdownJson from '../../../../../public/template/widgets/markdown/markdown.json?raw';
import markdownDx from '../../../../../public/template/widgets-dx/markdown/markdown.ts?raw';
import markdownPlaceholderJson from '../../../../../public/template/widgets/markdown/markdown-placeholder.json?raw';
import markdownPlaceholderDx from '../../../../../public/template/widgets-dx/markdown/markdown-placeholder.ts?raw';
import markdownHintJson from '../../../../../public/template/widgets/markdown/markdown-hint.json?raw';
import markdownHintDx from '../../../../../public/template/widgets-dx/markdown/markdown-hint.ts?raw';
import markdownCounterJson from '../../../../../public/template/widgets/markdown/markdown-counter.json?raw';
import markdownCounterDx from '../../../../../public/template/widgets-dx/markdown/markdown-counter.ts?raw';
import markdownAutogrowJson from '../../../../../public/template/widgets/markdown/markdown-autogrow.json?raw';
import markdownAutogrowDx from '../../../../../public/template/widgets-dx/markdown/markdown-autogrow.ts?raw';
import markdownToolsJson from '../../../../../public/template/widgets/markdown/markdown-tools.json?raw';
import markdownToolsDx from '../../../../../public/template/widgets-dx/markdown/markdown-tools.ts?raw';
import markdownTitlesJson from '../../../../../public/template/widgets/markdown/markdown-titles.json?raw';
import markdownTitlesDx from '../../../../../public/template/widgets-dx/markdown/markdown-titles.ts?raw';
import markdownDefaultOpenPreviewJson from '../../../../../public/template/widgets/markdown/markdown-default-open-preview.json?raw';
import markdownDefaultOpenPreviewDx from '../../../../../public/template/widgets-dx/markdown/markdown-default-open-preview.ts?raw';
import markdownAnatomyHtml from '../../../../../public/template/widgets/markdown/markdown-anatomy.html?raw';
export const configJson = 'markdown.json';
export const configDx = 'markdown.ts';
export const anatomy = 'markdown-anatomy.html';

Markdown widgets are [Input Fields](/widgets-reference/input-fields/overview) that provide a rich text editing experience with a formatting toolbar and an optional live preview panel. The toolbar lets users apply common markdown formatting (headings, bold, italic, quotes, links, lists) without needing to remember the syntax.

<DslCode dxCode={markdownDx} jsonCode={markdownJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown.json"
/>

## Dependencies

The Markdown widget requires a markdown parser to power its live preview. Since the parser is a third-party dependency, it is **not bundled** with the widget. Instead, you provide it at the form level via the `dependencies` property, keeping the core library decoupled from any specific parser.

<Aside type="tip">
  You can use any markdown parser that exposes a `(markdown: string) => string` function, such as [snarkdown](https://github.com/developit/snarkdown), [marked](https://github.com/markedjs/marked), or [micromark](https://github.com/micromark/micromark).
</Aside>

Pass `dependencies` as a property on the form component itself, not inside the widget's `props`:

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import snarkdown from 'snarkdown';
    import { Dependencies } from '@golemui/gui-shared';

    const deps: Dependencies = {
      markdown: {
        parse: (md: string) => snarkdown(md),
      },
    };

    <GolemForm
      formDef={formDef}
      data={formData}
      dependencies={deps}
      ...
    />
    ```

  </Fragment>
  <Fragment slot="angular">
    ```typescript
    import snarkdown from 'snarkdown';
    import { Dependencies } from '@golemui/gui-shared';

    protected deps: Dependencies = {
      markdown: {
        parse: (md: string) => snarkdown(md),
      },
    };
    ```
    ```html
    <gui-form
      [formDef]="formDef"
      [data]="formData"
      [dependencies]="deps"
      ...
    ></gui-form>
    ```

  </Fragment>
  <Fragment slot="lit">
    ```typescript
    import snarkdown from 'snarkdown';
    import { Dependencies } from '@golemui/gui-shared';

    const deps: Dependencies = {
      markdown: {
        parse: (md: string) => snarkdown(md),
      },
    };
    ```
    ```html
    <golem-form
      .formDef=${formDef}
      .data=${formData}
      .dependencies=${deps}
      ...
    ></golem-form>
    ```

  </Fragment>
</IntegrationTabs>

<Aside>The live preview will only render parsed HTML when a `dependencies.markdown.parse` function is provided. Without it, the toolbar and editor still work, but the preview panel will be empty.</Aside>

## Props

Use these `props` to customize your Markdown widget.

| prop                 | type              | description                                                                                                  |
| -------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------ |
| `placeholder`        | string            | A placeholder text to display when the editor has no value                                                   |
| `hint`               | string            | A description to display below the editor                                                                    |
| `counterMode`        | string            | Mode for the character counter if `maxLength` is set, possible values are `remaining` (default) or `current` |
| `minimumHeight`      | number            | Minimum height of the editor in pixels, defaults to 120                                                      |
| `autoGrow`           | boolean           | If true, the editor will automatically expand its height as the user types                                   |
| `defaultOpenPreview` | boolean           | If true, the split view preview panel is open by default                                                     |
| `tools`              | MarkdownButtons[] | An array of toolbar buttons to display. Defaults to all buttons                                              |
| `headingTitle`       | string            | Tooltip for the heading button                                                                               |
| `boldTitle`          | string            | Tooltip for the bold button                                                                                  |
| `italicTitle`        | string            | Tooltip for the italic button                                                                                |
| `strikethroughTitle` | string            | Tooltip for the strikethrough button                                                                         |
| `quoteTitle`         | string            | Tooltip for the quote button                                                                                 |
| `linkTitle`          | string            | Tooltip for the link button                                                                                  |
| `orderedListTitle`   | string            | Tooltip for the ordered list button                                                                          |
| `unorderedListTitle` | string            | Tooltip for the unordered list button                                                                        |
| `splitViewTitle`     | string            | Tooltip for the split view button                                                                            |
| `autocomplete`       | string            | `'on'`, `'off'`, or a space-separated list of W3C autofill tokens                                            |

### Toolbar Buttons

The `tools` prop lets you customize which toolbar buttons are displayed and their order. Available tokens:

| Token | Button         | Markdown syntax       |
| ----- | -------------- | --------------------- |
| `H`   | Heading        | `# Title`             |
| `B`   | Bold           | `**some text**`       |
| `I`   | Italic         | `_some text_`         |
| `Q`   | Quote          | `> some text`         |
| `L`   | Link           | `[some text](url)`    |
| `OL`  | Ordered list   | `1. some text`        |
| `UL`  | Unordered list | `- some text`         |
| `\|`  | Separator      | Visual separator only |

<DslCode dxCode={markdownToolsDx} jsonCode={markdownToolsJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-tools.json"
/>

### Titles

Use the title props to customize the tooltip text for each toolbar button.

<DslCode dxCode={markdownTitlesDx} jsonCode={markdownTitlesJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-titles.json"
/>

### Placeholder

Use the property `placeholder` to add a placeholder.

<DslCode dxCode={markdownPlaceholderDx} jsonCode={markdownPlaceholderJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-placeholder.json"
/>

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to add a description.

<DslCode dxCode={markdownHintDx} jsonCode={markdownHintJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-hint.json"
/>

### Character Counter

Use the validator `maxLength` to limit the number of characters and display a character counter. You can change how it counts with `counterMode` (`remaining` or `current`).

<DslCode dxCode={markdownCounterDx} jsonCode={markdownCounterJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-counter.json"
/>

### Auto Grow

Use the property `autoGrow` to allow the editor to expand automatically based on content. You can combine it with `minimumHeight` to set its initial size.

<DslCode dxCode={markdownAutogrowDx} jsonCode={markdownAutogrowJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-autogrow.json"
/>

### Default Open Preview

Use the property `defaultOpenPreview` to open the split view preview panel by default when the widget loads.

<DslCode dxCode={markdownDefaultOpenPreviewDx} jsonCode={markdownDefaultOpenPreviewJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/markdown/markdown-default-open-preview.json"
/>

## Styling

Markdown editors can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable             | Description                                     |
| ------------------------ | ----------------------------------------------- |
| `--gui-space-10`         | Textarea and preview top padding                |
| `--gui-space-3`          | Preview horizontal padding, toolbar padding     |
| `--gui-space-1`          | Toolbar button padding                          |
| `--gui-space-2`          | Toolbar button gap, counter top margin          |
| `--gui-font-sm`          | Counter text font size                          |
| `--gui-intent-error`     | Counter error color when max length is exceeded |
| `--gui-intent-primary`   | Active toolbar button background                |
| `--gui-radius-md`        | Widget border radius                            |
| `--gui-radius-sm`        | Toolbar button border radius                    |
| `--gui-border-default`   | Widget border, toolbar border, preview border   |
| `--gui-border-focus`     | Widget border color on focus                    |
| `--gui-shadow-focus`     | Widget box shadow on focus                      |
| `--gui-text-default`     | Text and toolbar icon fill color                |
| `--gui-bg-surface`       | Toolbar background color                        |
| `--gui-bg-surface-hover` | Toolbar button hover background                 |

### Anatomy

This is the anatomy of the Markdown Component in case you want to use your CSS styles.

<Code code={markdownAnatomyHtml} lang="html" title={anatomy} />
