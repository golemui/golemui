---
title: Dropdown
description: A Dropdown widget that allows users to pick an item from a list or search by typing
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import dropdownJson from '../../../../../public/template/widgets/dropdown/dropdown.json?raw';
import dropdownDx from '../../../../../public/template/widgets-dx/dropdown/dropdown.ts?raw';
import dropdownHintJson from '../../../../../public/template/widgets/dropdown/dropdown-hint.json?raw';
import dropdownHintDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-hint.ts?raw';
import dropdownCustomJson from '../../../../../public/template/widgets/dropdown/dropdown-custom.json?raw';
import dropdownCustomDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-custom.ts?raw';
import dropdownItemRendererJson from '../../../../../public/template/widgets/dropdown/dropdown-item-renderer.json?raw';
import dropdownItemRendererDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-item-renderer.ts?raw';
import dropdownSizeJson from '../../../../../public/template/widgets/dropdown/dropdown-size.json?raw';
import dropdownSizeDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-size.ts?raw';
import dropdownDebounceJson from '../../../../../public/template/widgets/dropdown/dropdown-debounce.json?raw';
import dropdownDebounceDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-debounce.ts?raw';
import dropdownAutocompleteJson from '../../../../../public/template/widgets/dropdown/dropdown-autocomplete.json?raw';
import dropdownAutocompleteDx from '../../../../../public/template/widgets-dx/dropdown/dropdown-autocomplete.ts?raw';
import dropdownAnatomyHtml from '../../../../../public/template/widgets/dropdown/dropdown-anatomy.html?raw';
export const configJson = 'dropdown.json';
export const configDx = 'dropdown.ts';
export const anatomy = 'dropdown-anatomy.html';

Dropdown widgets are [Input Fields](/widgets-reference/input-fields/overview) that provide a searchable list of items. Users can either type in the input to filter the list or select an item directly from the interactive dropdown menu.

The simplest way to populate `items` is a flat array of strings or numbers — the displayed label and the stored value are the same. When you need a separate stored value (e.g. an id behind a human-readable name), pass an array of objects and tell the widget which keys to use via [`labelField`](#using-objects) and [`valueField`](#using-objects).

<DslCode dxCode={dropdownDx} jsonCode={dropdownJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown.json"
  minHeight="500"
/>

## Props

Use these `props` to customize your Dropdown widget.

| prop            | type   | description                                                                                                                         |
| --------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| `items`         | array  | **Required**. Array of strings, numbers, or plain objects. See [Using Objects](#using-objects).                                     |
| `placeholder`   | string | Text shown in the input when it is empty                                                                                            |
| `hint`          | string | A description to display below the input                                                                                            |
| `labelField`    | string | When items are objects, the key whose value is shown to the user. Defaults to `label`.                                              |
| `valueField`    | string | When items are objects, the key whose value is stored in the form. Defaults to `value`. Required for selection to emit a primitive. |
| `searchFields`  | array  | List of fields to search on when filtering the list                                                                                 |
| `height`        | number | Height of the dropdown list container                                                                                               |
| `itemHeight`    | number | Height of each individual item in the list                                                                                          |
| `itemRenderer`  | string | Key of a registered renderer used to draw each item. See [Item Renderers](/features/item-renderers/)                                |
| `inputDebounce` | number | Delay in milliseconds before triggering a search/filter. Defaults to `500`                                                          |
| `autocomplete`  | string | `'on'`, `'off'`, or a space-separated list of W3C autofill tokens                                                                   |

### Using Objects

The top-of-page demo uses an array of strings — each entry is both what the user sees and what gets stored. That's enough for many forms, but it falls short when you need to keep a stable id behind a human-readable label (e.g. a country code paired with a country name).

For that case, pass `items` as plain objects and use `labelField` and `valueField` to tell the widget which key holds the displayed text and which one holds the stored value. The example below pairs each country `name` with a short `code`, and `searchFields` lets the user filter by either.

<DslCode dxCode={dropdownCustomDx} jsonCode={dropdownCustomJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-custom.json"
  minHeight="500"
/>

If your objects already use the default key names (`label` and `value`), you can omit `labelField` and `valueField` entirely. Otherwise both should be set so selection can extract the right primitive.

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to provide instructions for the user.

<DslCode dxCode={dropdownHintDx} jsonCode={dropdownHintJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-hint.json"
  minHeight="500"
/>

### Item Renderer

Use the property `itemRenderer` to draw each row with a registered renderer instead of the default template. The key must match one of the renderers registered on the form host — see [Item Renderers](/features/item-renderers/) for the full setup.

This demo uses the registered `countryItemRenderer` to display each country with its flag.

<DslCode dxCode={dropdownItemRendererDx} jsonCode={dropdownItemRendererJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-item-renderer.json"
  minHeight="500"
/>

### Size

Use `height` to set the size of the dropdown's scrollable list, and `itemHeight` to fix the row height used by the virtual scroller. Tune `itemHeight` whenever you use a custom `itemRenderer` whose rows are taller than the default.

<DslCode dxCode={dropdownSizeDx} jsonCode={dropdownSizeJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-size.json"
  minHeight="500"
/>

### Input Debounce

Use `inputDebounce` to control how long the dropdown waits after each keystroke before applying the filter. Useful when the `filter` event triggers a server-side search.

<DslCode dxCode={dropdownDebounceDx} jsonCode={dropdownDebounceJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-debounce.json"
  minHeight="500"
/>

### Autocomplete

Use `autocomplete` to set the underlying input's `autocomplete` attribute. Accepts `'on'`, `'off'`, or a space-separated list of [W3C autofill tokens](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).

<DslCode dxCode={dropdownAutocompleteDx} jsonCode={dropdownAutocompleteJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/dropdown/dropdown-autocomplete.json"
  minHeight="500"
/>

## Styling

Dropdowns can be styled as explained in the [Styling Guide](/styling/overview/).

### CSS Variables

Dropdowns leverage standard container and list variables.

| CSS Variable           | Description                     |
| ---------------------- | ------------------------------- |
| `--gui-radius-md`      | Border radius for the container |
| `--gui-border-default` | Default border color            |

### Anatomy

This is the anatomy of the Dropdown Widget in case you want to use your CSS styles.

<Code code={dropdownAnatomyHtml} lang="html" title={anatomy} />
