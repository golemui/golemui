---
title: Toggle
description: A Toggle widget to allow user to switch between two states
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import toggleJson from '../../../../../public/template/widgets/toggle/toggle.json?raw';
import toggleDx from '../../../../../public/template/widgets-dx/toggle/toggle.ts?raw';
import toggleHintJson from '../../../../../public/template/widgets/toggle/toggle-hint.json?raw';
import toggleHintDx from '../../../../../public/template/widgets-dx/toggle/toggle-hint.ts?raw';
import togglePositionJson from '../../../../../public/template/widgets/toggle/toggle-position.json?raw';
import togglePositionDx from '../../../../../public/template/widgets-dx/toggle/toggle-position.ts?raw';
import toggleAnatomyHtml from '../../../../../public/template/widgets/toggle/toggle-anatomy.html?raw';
export const configJson = 'toggle.json';
export const configDx = 'toggle.ts';
export const anatomy = 'toggle-anatomy.html';

Toggle widgets are [Input Fields](/widgets-reference/input-fields/overview) that act as visual switches, allowing the user to turn a setting on or off. They represent a boolean value.

<DslCode dxCode={toggleDx} jsonCode={toggleJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/toggle/toggle.json"
/>

## Props

Use these `props` to customize your Toggle widget.

| prop             | type   | description                                                                                           |
| ---------------- | ------ | ----------------------------------------------------------------------------------------------------- |
| `hint`           | string | A description to display below the toggle control                                                     |
| `togglePosition` | string | Position of the toggle switch relative to its label. Possible values are `left` (default) or `right`. |

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to add a description.

<DslCode dxCode={toggleHintDx} jsonCode={toggleHintJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/toggle/toggle-hint.json"
/>

### Position

Use the property `togglePosition` to align the toggle switch to the `left` (default) or `right` of its label.

<DslCode dxCode={togglePositionDx} jsonCode={togglePositionJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/toggle/toggle-position.json"
/>

## Styling

Toggles can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable                    | Description                                               |
| ------------------------------- | --------------------------------------------------------- |
| `--gui-toggle-width`            | Total width of the toggle switch                          |
| `--gui-toggle-height`           | Total height of the toggle switch                         |
| `--gui-toggle-slider-width`     | Width of the round slider knob thumb                      |
| `--gui-toggle-slider-height`    | Height of the round slider knob thumb                     |
| `--gui-toggle-slider-transform` | X-axis transform value distance for the knob when checked |
| `--gui-bg-default`              | Background color of slider container when off             |
| `--gui-bg-disabled`             | Background color of slider container when disabled        |
| `--gui-border-default`          | Default border of the off-state slider background         |
| `--gui-intent-primary`          | Slider background and border color when checked / on      |
| `--gui-text-default`            | Border color when focused                                 |
| `--gui-intent-error`            | Border and shadow color when invalid                      |

### Anatomy

This is the anatomy of the Toggle Widget in case you want to use your CSS styles.

<Code code={toggleAnatomyHtml} lang="html" title={anatomy} />
