---
title: Password
description: A Password widget to allow user to enter secure string chains
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import passwordJson from '../../../../../public/template/widgets/password/password.json?raw';
import passwordDx from '../../../../../public/template/widgets-dx/password/password.ts?raw';
import passwordPlaceholderJson from '../../../../../public/template/widgets/password/password-placeholder.json?raw';
import passwordPlaceholderDx from '../../../../../public/template/widgets-dx/password/password-placeholder.ts?raw';
import passwordHintJson from '../../../../../public/template/widgets/password/password-hint.json?raw';
import passwordHintDx from '../../../../../public/template/widgets-dx/password/password-hint.ts?raw';
import passwordIconJson from '../../../../../public/template/widgets/password/password-icon.json?raw';
import passwordIconDx from '../../../../../public/template/widgets-dx/password/password-icon.ts?raw';
import passwordAnatomyHtml from '../../../../../public/template/widgets/password/password-anatomy.html?raw';
export const configJson = 'password.json';
export const configDx = 'password.ts';
export const anatomy = 'password-anatomy.html';

Password widgets are [Input Fields](/widgets-reference/input-fields/overview) that allow the user to enter hidden string chains, commonly used for sensitive information.

<DslCode dxCode={passwordDx} jsonCode={passwordJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/password/password.json"
/>

## Props

Use these `props` to customize your Password widget.

| prop                | type   | description                                                          |
| ------------------- | ------ | -------------------------------------------------------------------- |
| `placeholder`       | string | A placeholder text to display when the password input has no value   |
| `hint`              | string | A description to display below the password input                    |
| `icon`              | string | A css class to display an icon inside the input                      |
| `showPasswordIcon`  | string | Icon indicating the state to show the password (e.g. eye icon)       |
| `hidePasswordIcon`  | string | Icon indicating the state to hide the password (e.g. eye-slash icon) |
| `showPasswordLabel` | string | Label text for the show password toggle, visible if no icon is set   |
| `hidePasswordLabel` | string | Label text for the hide password toggle, visible if no icon is set   |
| `autocomplete`      | string | `'on'`, `'off'`, or a space-separated list of W3C autofill tokens    |

### Placeholder

Use the property `placeholder` to add a placeholder.

<DslCode dxCode={passwordPlaceholderDx} jsonCode={passwordPlaceholderJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/password/password-placeholder.json"
/>

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to add a description.

<DslCode dxCode={passwordHintDx} jsonCode={passwordHintJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/password/password-hint.json"
/>

### Icon

Use the property `icon` to add an icon to the `password`. The value of `icon` represents a set of CSS classes separated by spaces. The icon is displayed on the left by default. The widget also has built-in icons and labels to toggle password visibility.

<DslCode dxCode={passwordIconDx} jsonCode={passwordIconJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/widgets/password/password-icon.json"
/>

## Styling

Passwords can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable                | Description                             |
| --------------------------- | --------------------------------------- |
| `--gui-space-gap--inner`    | Label gap and margin bottom             |
| `--gui-padding`             | Input padding                           |
| `--gui-color-border`        | Input border color                      |
| `--gui-radius`              | Input radius                            |
| `--gui-color-bg`            | Input background color                  |
| `--gui-color-fg`            | Input text color                        |
| `--gui-color-primary`       | Input border color and outline on focus |
| `--gui-color-error`         | Input border color when invalid         |
| `--gui-space-gap`           | Icon left padding                       |
| `--gui-icon-space-gap`      | Icon right padding                      |
| `--gui-font-size--small`    | Hint font size                          |
| `--gui-color-fg--secondary` | Hint font color                         |
| `--gui-text-default`        | Eye icon toggle color                   |

### Anatomy

This is the anatomy of the Password Component in case you want to use your CSS styles.

<Code code={passwordAnatomyHtml} lang="html" title={anatomy} />
