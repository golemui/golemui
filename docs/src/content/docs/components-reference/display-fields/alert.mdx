---
title: Alert
description: A Display component for showing status messages, notifications, or feedback to the user
---
import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import alertJson from '../../../../../public/template/components/alert/alert.json?raw';
import alertInfoJson from '../../../../../public/template/components/alert/alert-info.json?raw';
import alertLevelsJson from '../../../../../public/template/components/alert/alert-levels.json?raw';
import alertAnatomyHtml from '../../../../../public/template/components/alert/alert-anatomy.html?raw';
export const configJson = 'alert.json';
export const anatomy = 'alert-anatomy.html';

Alert components are [Display Fields](/components-reference/display-fields/overview) used to provide contextual feedback or record information for the user. They are non-interactive and purely informational.

<Code code={alertJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/alert/alert.json"
/>

## Props

Use these `props` to customize your Alert component.

| prop    | type   | description                                                                                           |
| ------- | ------ | ----------------------------------------------------------------------------------------------------- |
| `text`  | string | **Required**. The message to display in the alert                                                     |
| `level` | string | Visual severity of the alert: `default`, `info`, `success`, `warning`, `error`. Defaults to `default` |

### Alert Levels

Use the `level` property to change the visual importance and color scheme of the alert.

<Code code={alertLevelsJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/alert/alert-levels.json"
/>

## Styling

Alerts can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables used to style different severity levels.

| CSS Variable           | Description                      |
| ---------------------- | -------------------------------- |
| `--gui-intent-info`    | Border color for info alerts     |
| `--gui-intent-info-bg` | Background color for info alerts |
| `--gui-intent-success` | Border color for success alerts  |
| `--gui-intent-warning` | Border color for warning alerts  |
| `--gui-intent-error`   | Border color for error alerts    |
| `--gui-radius-md`      | Border radius for the alert box  |

### Anatomy

This is the anatomy of the Alert Component in case you want to use your CSS styles.

<Code code={alertAnatomyHtml} lang="html" title={anatomy} />
