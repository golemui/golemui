---
title: Textarea
description: A Textarea component to allow user to enter multi-line string chains
---
import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import textareaJson from '../../../../../public/template/components/textarea/textarea.json?raw';
import textareaPlaceholderJson from '../../../../../public/template/components/textarea/textarea-placeholder.json?raw';
import textareaHintJson from '../../../../../public/template/components/textarea/textarea-hint.json?raw';
import textareaIconJson from '../../../../../public/template/components/textarea/textarea-icon.json?raw';
import textareaCounterJson from '../../../../../public/template/components/textarea/textarea-counter.json?raw';
import textareaAutogrowJson from '../../../../../public/template/components/textarea/textarea-autogrow.json?raw';
import textareaAnatomyHtml from '../../../../../public/template/components/textarea/textarea-anatomy.html?raw';
export const configJson = 'textarea.json';
export const anatomy = 'textarea-anatomy.html';

Textarea components are [Input Fields](/components-reference/input-fields/overview) that allow the user to enter multi-line string chains.

<Code code={textareaJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea.json"
/>

## Props

Use these `props` to customize your Textarea component.

| prop | type | description |
| ---- | ---- | ----------- |
| `placeholder` | string | A placeholder text to display when the textarea has no value |
| `hint` | string | A description to display below the textarea |
| `icon` | string | A css class to display an icon inside the textarea |
| `counterMode` | string | Mode for the character counter if `maxLength` is set, possible values are `remaining` (default) or `current` |
| `minimumHeight` | number | Minimum height of the textarea in pixels, defaults to 120 |
| `autoGrow` | boolean | If true, the textarea will automatically expand its height as the user types |
| `maxLength` | number | Maximum number of characters allowed. Also enables the character counter |

### Placeholder

Use the property `placeholder` to add a placeholder.

<Code code={textareaPlaceholderJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea-placeholder.json"
/>

### Hint

> [!NOTE]
> A `label` is required for the `hint` to be displayed.

Use the property `hint` to add a description.

<Code code={textareaHintJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea-hint.json"
/>

### Icon

Use the property `icon` to add an icon to the `textarea`. The value of `icon` represents a set of CSS classes separated by spaces. The icon is displayed inside the textarea on the left.

<Code code={textareaIconJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea-icon.json"
/>

### Character Counter

Use the property `maxLength` to limit the number of characters and display a character counter. You can change how it counts with `counterMode` (`remaining` or `current`).

<Code code={textareaCounterJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea-counter.json"
/>

### Auto Grow

Use the property `autoGrow` to allow the textarea to expand automatically based on content. You can combine it with `minimumHeight` to set its initial size.

<Code code={textareaAutogrowJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/textarea/textarea-autogrow.json"
/>

## Styling

Textareas can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable | Description |
| ------------ | ----------- |
| `--gui-space-10` | Icon left padding or inline-start space |
| `--gui-font-sm` | Counter text font size |
| `--gui-space-2` | Counter top margin |
| `--gui-intent-error` | Counter error color when max length is exceeded |
| `--gui-space-gap--inner` | Label gap and margin bottom |
| `--gui-padding` | Input padding |
| `--gui-color-border` | Input border color |
| `--gui-radius` | Input radius |
| `--gui-color-bg` | Input background color |
| `--gui-color-fg` | Input text color |
| `--gui-color-primary` | Input border color and outline on focus |
| `--gui-color-error` | Input border color when invalid |

### Anatomy

This is the anatomy of the Textarea Component in case you want to use your CSS styles.

<Code code={textareaAnatomyHtml} lang="html" title={anatomy} />
