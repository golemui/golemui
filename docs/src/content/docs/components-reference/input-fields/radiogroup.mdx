---
title: Radio Group
description: A Radio Group component for selecting a single option from a list of predefined choices
---
import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import radiogroupJson from '../../../../../public/template/components/radiogroup/radiogroup.json?raw';
import radiogroupHintJson from '../../../../../public/template/components/radiogroup/radiogroup-hint.json?raw';
import radiogroupCustomJson from '../../../../../public/template/components/radiogroup/radiogroup-custom.json?raw';
import radiogroupAnatomyHtml from '../../../../../public/template/components/radiogroup/radiogroup-anatomy.html?raw';
export const configJson = 'radiogroup.json';
export const anatomy = 'radiogroup-anatomy.html';

Radio Group components are [Input Fields](/components-reference/input-fields/overview) that present a set of mutually exclusive options. Users can select exactly one option from the group.

<Code code={radiogroupJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/radiogroup/radiogroup.json"
/>

## Props

Use these `props` to customize your Radio Group component.

| prop         | type             | description                                                                    |
| ------------ | ---------------- | ------------------------------------------------------------------------------ |
| `options`    | array            | **Required**. Collection of options to display                                 |
| `value`      | string \| number | The value of the currently selected option                                     |
| `hint`       | string           | Description text displayed below the label                                     |
| `labelField` | string           | Custom key to use for the display label of each option. Defaults to `label`    |
| `valueField` | string           | Custom key to use for the underlying value of each option. Defaults to `value` |

### Hint

Use the property `hint` to provide instructions for the user.

<Code code={radiogroupHintJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/radiogroup/radiogroup-hint.json"
/>

### Custom Fields

You can map different fields for the options using `labelField` and `valueField`.

<Code code={radiogroupCustomJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/radiogroup/radiogroup-custom.json"
/>

## Styling

Radio Groups can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable           | Description                              |
| ---------------------- | ---------------------------------------- |
| `--gui-intent-primary` | Color for the inner circle when selected |

### Anatomy

This is the anatomy of the Radio Group Component in case you want to use your CSS styles.

<Code code={radiogroupAnatomyHtml} lang="html" title={anatomy} />
