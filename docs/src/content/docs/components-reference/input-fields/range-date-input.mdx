---
title: Range Date Input
description: A Range Date Input component to allow users to enter multiple date ranges via keyboard using segmented inputs
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import rangeDateInputJson from '../../../../../public/template/components/range-date-input/range-date-input.json?raw';
import rangeDateInputHintJson from '../../../../../public/template/components/range-date-input/range-date-input-hint.json?raw';
import rangeDateInputIconJson from '../../../../../public/template/components/range-date-input/range-date-input-icon.json?raw';
import rangeDateInputSeparatorJson from '../../../../../public/template/components/range-date-input/range-date-input-separator.json?raw';
import rangeDateInputAriaLabelsJson from '../../../../../public/template/components/range-date-input/range-date-input-aria-labels.json?raw';
import rangeDateInputAnatomyHtml from '../../../../../public/template/components/range-date-input/range-date-input-anatomy.html?raw';
export const configJson = 'range-date-input.json';
export const anatomy = 'range-date-input-anatomy.html';

Range Date Input components are [Input Fields](/components-reference/input-fields/overview) that allow the user to enter multiple date ranges using segmented keyboard inputs. Each completed range is displayed as a pill that can be removed. The date segments automatically localize based on the user's region and locale.

<Code code={rangeDateInputJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/range-date-input/range-date-input.json"
/>

## Props

Use these `props` to customize your Range Date Input component.

| prop                   | type   | description                                                       |
| ---------------------- | ------ | ----------------------------------------------------------------- |
| `hint`                 | string | A description to display below the input                          |
| `icon`                 | string | A css class to display an icon inside the input                   |
| `separator`            | string | The separator between start and end date fields. Defaults to `-`  |
| `removePillAriaLabel`  | string | Aria label for the remove pill action. Defaults to `Remove date`  |
| `startDateAriaLabel`   | string | Aria label for the start date field group. Defaults to `Start date` |
| `endDateAriaLabel`     | string | Aria label for the end date field group. Defaults to `End date`   |

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to add a description or validation instructions.

<Code code={rangeDateInputHintJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/range-date-input/range-date-input-hint.json"
/>

### Icon

Use the property `icon` to add an icon to the component. The value of `icon` represents a set of CSS classes separated by spaces. The icon is displayed inside the input border.

<Code code={rangeDateInputIconJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/range-date-input/range-date-input-icon.json"
/>

### Separator

Use the property `separator` to customize the text displayed between the start date and end date fields. By default the separator is `-`.

<Code code={rangeDateInputSeparatorJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/range-date-input/range-date-input-separator.json"
/>

### Aria Labels

Use the properties `removePillAriaLabel`, `startDateAriaLabel` and `endDateAriaLabel` to customize the accessibility labels for screen readers. These properties are localizable.

<Code code={rangeDateInputAriaLabelsJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/range-date-input/range-date-input-aria-labels.json"
/>

## Styling

Range Date Inputs can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable              | Description                                                                |
| ------------------------- | -------------------------------------------------------------------------- |
| `--gui-intent-error`      | Border and focus shadow color when invalid                                 |
| `--gui-border-focus`      | Border color when a pill or input is focused                               |
| `--gui-shadow-focus`      | Box shadow when a pill or input is focused                                 |
| `--gui-intent-primary`    | Pill background color                                                      |
| `--gui-color-white`       | Pill text and remove button color                                          |
| `--gui-border-default`    | Pills wrapper border and separator border color                            |
| `--gui-bg-default`        | Scroll shadow gradient color and dropdown background color                 |
| `--gui-text-default`      | Active piece (day/month/year) visual underline color                       |
| `--gui-radius-md`         | Pill and dropdown border radius                                            |
| `--gui-radius-full`       | Pill count button and remove button border radius                          |

### Anatomy

This is the anatomy of the Range Date Input Component in case you want to use your CSS styles.

<Code code={rangeDateInputAnatomyHtml} lang="html" title={anatomy} />
