---
title: Repeater
description: A Repeater component to allow the user to enter information as a list
---
import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import DslCode from '../../../../components/DslCode.astro';
import repeaterJson from '../../../../../public/template/components/repeater/repeater.json?raw';
import repeaterDx from '../../../../../public/template/components-dx/repeater/repeater.ts?raw';
import repeaterLabelsJson from '../../../../../public/template/components/repeater/repeater-labels.json?raw';
import repeaterLabelsDx from '../../../../../public/template/components-dx/repeater/repeater-labels.ts?raw';
import repeaterLimitJson from '../../../../../public/template/components/repeater/repeater-limit.json?raw';
import repeaterLimitDx from '../../../../../public/template/components-dx/repeater/repeater-limit.ts?raw';
import repeaterTitleJson from '../../../../../public/template/components/repeater/repeater-title.json?raw';
import repeaterTitleDx from '../../../../../public/template/components-dx/repeater/repeater-title.ts?raw';
import repeaterIconsJson from '../../../../../public/template/components/repeater/repeater-icons.json?raw';
import repeaterIconsDx from '../../../../../public/template/components-dx/repeater/repeater-icons.ts?raw';
import repeaterNestedJson from '../../../../../public/template/components/repeater/repeater-nested.json?raw';
import repeaterNestedDx from '../../../../../public/template/components-dx/repeater/repeater-nested.ts?raw';
import repeaterAnatomyHtml from '../../../../../public/template/components/repeater/repeater-anatomy.html?raw';
export const configJson = 'repeater.json';
export const configDx = 'repeater.ts';
export const anatomy = 'repeater.html';

Repeater components are [Input Fields](/components-reference/input-fields/overview) that allow the user to enter information as a list given a template. The information will be stored and returned as an array of objects.

<DslCode dxCode={repeaterDx} jsonCode={repeaterJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater.json"
/>

## Props

Use this `props` to customize your Repeater component.

| prop               | type   | value                                                                     |
| ------------------ | ------ | ------------------------------------------------------------------------- |
| `template`         | object | **MANDATORY** A template Field with the elements rendered by the repeater |
| `addLabel`         | string | Label text for the add button                                             |
| `removeLabel`      | string | Label text for the remove button                                          |
| `limit`            | number | Maximum number of items that can be added                                 |
| `title`            | string | Optional title displayed at the top of each card                          |
| `addButtonIcon`    | string | CSS class string for the icon displayed in the add button                 |
| `removeButtonIcon` | string | CSS class string for the icon displayed in the remove button              |

### Template

Use the property `template` to render a tree of Field components for each item in the array of the Repeater component. The `path` in the [Input Fields](/components-reference/input-fields/overview) components inside the `template` will be updated replacing `items` keyword by the index of the array. For example, `users.items.firstName` will be replaced by `users[0].firstName` for the first element and so on.

<DslCode dxCode={repeaterDx} jsonCode={repeaterJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater.json"
/>

### Add and Remove Labels

Use the property `addLabel` and `removeLabel` to set custom labels for the Add and Remove button.

<DslCode dxCode={repeaterLabelsDx} jsonCode={repeaterLabelsJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater-labels.json"
/>

### Limit

Use the property `limit` to set a limit to the number of elements that can be added to a repeater.

<DslCode dxCode={repeaterLimitDx} jsonCode={repeaterLimitJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater-limit.json"
/>

### Card Title

Use the property `title` to display a static title at the top of each card.

<DslCode dxCode={repeaterTitleDx} jsonCode={repeaterTitleJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater-title.json"
/>

### Button Icons

Use the properties `addButtonIcon` and `removeButtonIcon` to display an icon inside the add and remove buttons. Icons are passed as a CSS class string, the same way as other icon props in the library.

<DslCode dxCode={repeaterIconsDx} jsonCode={repeaterIconsJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater-icons.json"
/>

### Nested Repeaters

Repeaters can be nested inside other repeaters to model hierarchical data structures. Each nesting level adds an additional `.items.` segment to the `path`. For example, in a Teams → Members structure:

- The outer repeater path is `teams`
- Fields inside the outer template use `teams.items.fieldName`
- The inner repeater path is `teams.items.members`
- Fields inside the inner template use `teams.items.members.items.fieldName`

At runtime, each `items` token is replaced by the corresponding array index. For instance, `teams.items.members.items.memberName` becomes `teams[0].members[1].memberName` for the second member of the first team.

<DslCode dxCode={repeaterNestedDx} jsonCode={repeaterNestedJson} dxTitle={configDx} jsonTitle={configJson} />
<Demo
  src="/template/index.html?form=template/components/repeater/repeater-nested.json"
/>

## Styling

Repeaters can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable         | Description                         |
| -------------------- | ----------------------------------- |
| `--gui-space-gap`    | Space between each repeater element |
| `--gui-field-gap`    | Space between Add/Remove buttons    |
| `--gui-padding`      | Repeater element padding            |
| `--gui-color-border` | Repeater element border color       |
| `--gui-radius`       | Repeater element border radius      |
| `--gui-color-bg`     | Repeater element background color   |
| `--gui-color-fg`     | Repeater element text color         |

### Anatomy

This is the anatomy of the Repeater Component in case you want to use your CSS styles.

<Code code={repeaterAnatomyHtml} lang="html" title={anatomy} />
