---
title: Date Input
description: A Date Input component to allow user to enter a date directly via keyboard using segmented inputs
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Aside, Code } from '@astrojs/starlight/components';
import dateinputJson from '../../../../../public/template/components/dateinput/dateinput.json?raw';
import dateinputHintJson from '../../../../../public/template/components/dateinput/dateinput-hint.json?raw';
import dateinputIconJson from '../../../../../public/template/components/dateinput/dateinput-icon.json?raw';
import dateinputAnatomyHtml from '../../../../../public/template/components/dateinput/dateinput-anatomy.html?raw';
export const configJson = 'dateinput.json';
export const anatomy = 'dateinput-anatomy.html';

Date Input components are [Input Fields](/components-reference/input-fields/overview) that provide a structured, segmented text-input approach to manual date entry, offering a keyboard-friendly alternative to interactive calendars. The date segments automatically localize based on the user's region and locale.

<Code code={dateinputJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/dateinput/dateinput.json"
/>

## Props

Use these `props` to customize your Date Input component.

| prop   | type   | description                                         |
| ------ | ------ | --------------------------------------------------- |
| `hint` | string | A description to display below the segmented inputs |
| `icon` | string | A css class to display an icon inside the input     |

### Hint

<Aside>A `label` is required for the `hint` to be displayed.</Aside>

Use the property `hint` to add a description or validation instructions.

<Code code={dateinputHintJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/dateinput/dateinput-hint.json"
/>

### Icon

Use the property `icon` to add an icon to the component. The value of `icon` represents a set of CSS classes separated by spaces. The icon is displayed inside the input border.

<Code code={dateinputIconJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/dateinput/dateinput-icon.json"
/>

## Styling

Date Inputs can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables and a quick description of what you will style.

| CSS Variable         | Description                                                                    |
| -------------------- | ------------------------------------------------------------------------------ |
| `--gui-intent-error` | Border and focus shadow color when invalid                                     |
| `--gui-text-default` | Active piece (day/month/year) visual underline color and standard focus shadow |

### Anatomy

This is the anatomy of the Date Input Component in case you want to use your CSS styles.

<Code code={dateinputAnatomyHtml} lang="html" title={anatomy} />
