---
title: Date Picker
description: A Date Picker component to allow user to enter a date via keyboard or by picking from an interactive calendar
---

import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import datePickerJson from '../../../../../public/template/components/date-picker/date-picker.json?raw';
import datePickerPlaceholderJson from '../../../../../public/template/components/date-picker/date-picker-placeholder.json?raw';
import datePickerHintJson from '../../../../../public/template/components/date-picker/date-picker-hint.json?raw';
import datePickerIconJson from '../../../../../public/template/components/date-picker/date-picker-icon.json?raw';
import datePickerFormatJson from '../../../../../public/template/components/date-picker/date-picker-format.json?raw';
import datePickerAnatomyHtml from '../../../../../public/template/components/date-picker/date-picker-anatomy.html?raw';
export const configJson = 'date-picker.json';
export const anatomy = 'date-picker-anatomy.html';

Date Picker components are [Input Fields](/components-reference/input-fields/overview) that combine the functionality of a [Date Input](/components-reference/input-fields/dateinput) and a [Calendar](/components-reference/input-fields/calendar). They allow the user to swiftly type a date using their keyboard or to open a drop-down calendar popover to select a date visually.

<Code code={datePickerJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/date-picker/date-picker.json"
  minHeight="500"
/>

## Props

Use these `props` to customize your Date Picker component. The Date Picker inherits all data-entry properties from the Date Input and formatting options from the Calendar.

| prop                 | type   | description                                                                     |
| -------------------- | ------ | ------------------------------------------------------------------------------- |
| `placeholder`        | string | Accessible placeholder text for the component                                   |
| `hint`               | string | A description to display below the input fields                                 |
| `icon`               | string | Custom icon class to display inside the input visually (e.g. `fas fa-calendar`) |
| `dayFormat`          | string | Formatting style for days (`numeric` or `2-digit`)                              |
| `monthFormat`        | string | Formatting style for months (`numeric`, `2-digit`, `long`, `short`, `narrow`)   |
| `weekdayFormat`      | string | Formatting style for weekdays (`short`, `long`, `narrow`)                       |
| `prevMonthIcon`      | string | Custom CSS class for the "previous month" button inside the calendar            |
| `nextMonthIcon`      | string | Custom CSS class for the "next month" button inside the calendar                |
| `prevMonthAriaLabel` | string | ARIA label for the previous month button                                        |
| `nextMonthAriaLabel` | string | ARIA label for the next month button                                            |

### Placeholder

Use the property `placeholder` to provide context via an accessible placeholder string explicitly. Note that individual date segments (dd, mm, yyyy) always show format placeholders by default.

<Code code={datePickerPlaceholderJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/date-picker/date-picker-placeholder.json"
  minHeight="500"
/>

### Hint

> [!NOTE]
> A `label` is required for the `hint` to be displayed.

Use the property `hint` to add descriptive text.

<Code code={datePickerHintJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/date-picker/date-picker-hint.json"
  minHeight="500"
/>

### Icon

Use the property `icon` to add an icon, which appears next to the toggle chevron.

<Code code={datePickerIconJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/date-picker/date-picker-icon.json"
  minHeight="500"
/>

### Popover Calendar Formatting

Use properties like `dayFormat`, `monthFormat` and `weekdayFormat` to control the display of the interactive calendar popover.

<Code code={datePickerFormatJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/date-picker/date-picker-format.json"
  minHeight="500"
/>

## Styling

Date Pickers can be styled as explained in the [Styling Guide](/guides/styling). Because they are composite components, they utilize variables from `textinput`, `dateinput`, and `calendar`.

### CSS Variables

Following you will find a list with the CSS Variables specific to the Date Picker popover positioning and toggle.

| CSS Variable             | Description                                  |
| ------------------------ | -------------------------------------------- |
| `--gui-radius-md`        | Interactive calendar container border radius |
| `--gui-bg-default`       | Interactive calendar background color        |
| `--gui-border-default`   | Interactive calendar border color            |
| `--gui-space-3`          | Internal padding within the calendar view    |
| `--gui-space-gap--inner` | Label gap                                    |

### Anatomy

This is the anatomy of the Date Picker Component in case you want to use your CSS styles.

<Code code={datePickerAnatomyHtml} lang="html" title={anatomy} />
