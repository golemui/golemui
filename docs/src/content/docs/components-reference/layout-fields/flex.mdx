---
title: Flex
description: A Layout component for creating flexible, responsive grid-like structures
---
import Demo from '../../../../../src/components/landing/Demo.astro';

import { Code } from '@astrojs/starlight/components';
import flexJson from '../../../../../public/template/components/flex/flex.json?raw';
import flexHorizontalJson from '../../../../../public/template/components/flex/flex-horizontal.json?raw';
import flexRowReverseJson from '../../../../../public/template/components/flex/flex-row-reverse.json?raw';
import flexColumnReverseJson from '../../../../../public/template/components/flex/flex-column-reverse.json?raw';
import flexHorizontalAlignmentJson from '../../../../../public/template/components/flex/flex-horizontal-alignment.json?raw';
import flexNestedAlignmentJson from '../../../../../public/template/components/flex/flex-nested-alignment.json?raw';
import flexAnatomyHtml from '../../../../../public/template/components/flex/flex-anatomy.html?raw';
export const configJson = 'flex.json';
export const anatomy = 'flex-anatomy.html';

Flex components are [Layout Fields](/components-reference/layout-fields/overview) that use CSS Flexbox to arrange child widgets using standard CSS `flex-direction` values. They are the primary tool for building responsive form layouts.

<Code code={flexJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/flex/flex.json"
/>

## Props

Use these `props` to customize your Flex component.

| prop        | type   | description                                                                                                                                          |
| ----------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `direction` | string | CSS flex-direction: `'row'`, `'row-reverse'`, `'column'`, or `'column-reverse'`. Defaults to `'column'`                                              |
| `justify`   | string | Cross-axis alignment. In a **row**: vertical alignment of items (`align-items`). In a **column**: horizontal alignment of items (`justify-items`). Values: `'start'`, `'end'`, `'center'`, `'stretch'`. Defaults to `'stretch'` |
| `align`     | string | Main-axis distribution. In a **row**: horizontal spacing (`justify-content`). In a **column**: vertical spacing (`justify-content`). Values: `'start'`, `'end'`, `'center'`, `'space-between'`, `'space-around'`, `'space-evenly'`. Defaults to `'start'` |
| `gap`       | number | Space between children in pixels                                                                                                                     |

### Row Layout

Set `direction` to `"row"` to place child widgets side-by-side. On small screens (max-width: 480px), row layouts automatically stack vertically for better mobile usability.

<Code code={flexHorizontalJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/flex/flex-horizontal.json"
/>

### Row Reverse Layout

Set `direction` to `"row-reverse"` to place child widgets side-by-side in reversed order.

<Code code={flexRowReverseJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/flex/flex-row-reverse.json"
/>

### Column Reverse Layout

Set `direction` to `"column-reverse"` to stack child widgets vertically in reversed order.

<Code code={flexColumnReverseJson} lang="js" title={configJson} />
<Demo
  src="/template/index.html?form=template/components/flex/flex-column-reverse.json"
/>

### Horizontal Alignment

Use `align` and `justify` together in a `row` layout to control both axes. `align` distributes items horizontally (main axis) and `justify` aligns them vertically (cross axis).

In this example, items are horizontally centered with `align: "center"` and vertically centered with `justify: "center"`:

<Code code={flexHorizontalAlignmentJson} lang="js" title="flex-horizontal-alignment.json" />
<Demo
  src="/template/index.html?form=template/components/flex/flex-horizontal-alignment.json"
/>

### Nested Alignment

Nest flex widgets to combine row and column layouts with independent alignment on each level. The outer `row` flex uses `justify: "center"` to vertically center the inner columns; each inner `column` flex uses `align` to control how its children distribute vertically.

<Code code={flexNestedAlignmentJson} lang="js" title="flex-nested-alignment.json" />
<Demo
  src="/template/index.html?form=template/components/flex/flex-nested-alignment.json"
/>

## Styling

Flex layouts can be styled as explained in the [Styling Guide](/guides/styling).

### CSS Variables

Following you will find a list with the CSS Variables used to control spacing within the Flex component.

| CSS Variable    | Description                 |
| --------------- | --------------------------- |
| `--gui-space-4` | Default gap between widgets |

### Anatomy

This is the anatomy of the Flex Component in case you want to use your CSS styles.

<Code code={flexAnatomyHtml} lang="html" title={anatomy} />
