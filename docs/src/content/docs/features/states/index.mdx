---
title: States
description: Declare named conditions and reuse them across your form to gate widgets, override properties, and compose behavior.
sidebar:
  order: 0
---

import { Aside } from '@astrojs/starlight/components';
import DslTabs from '../../../../components/DslTabs.astro';
import IntegrationTabs from '../../../../components/IntegrationTabs.astro';

States are **named boolean expressions** evaluated against the live form data. They let you describe complex conditional behavior once and reuse it across many widgets — without scattering inline conditionals throughout your form definition.

A state is a key-value pair: the **key** is the name you reference from `include` / `exclude` rules, prop overrides, and event handlers; the **value** is an expression string that reads from `$form.<path>`.

<DslTabs>
  <Fragment slot="dx">

```ts
const states = {
  hasDiscount: '$form.hasDiscountCode === true',
  isAdult: '$form.driverAge >= 18',
};
```

  </Fragment>
  <Fragment slot="json">

```json
{
  "states": {
    "hasDiscount": "$form.hasDiscountCode === true",
    "isAdult": "$form.driverAge >= 18"
  },
  "form": [
    /* ... */
  ]
}
```

  </Fragment>
</DslTabs>

## Declaring states

<DslTabs>
  <Fragment slot="dx">

In the Programmatic API, states live inside `formConfig.states`. The form component reads them through `formConfig`, so the engine sees the same `$form` data the form itself binds to.

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import { gui } from '@golemui/gui-shared';
    import * as React from '@golemui/gui-react';

    const formDef = [
      gui.inputs.booleanInput('hasDiscountCode', { label: 'I have a discount code' }),
      gui.inputs.numberInput('driverAge', { label: 'Driver age' }),
      gui.inputs.textInput('discountCode', {
        label: 'Discount code',
        include: { in: ['hasDiscount'] },
      }),
    ];

    const formConfig = {
      widgetLoaders: React.widgetLoaders,
      states: {
        hasDiscount: '$form.hasDiscountCode === true',
        isAdult: '$form.driverAge >= 18',
      },
    };

    const config = { formDef, data: {}, formConfig };

    export function MyForm() {
      return <React.GuiForm config={config} />;
    }
    ```

  </Fragment>
  <Fragment slot="angular">
    ```ts
    import { Component } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { gui } from '@golemui/gui-shared';
    import * as Angular from '@golemui/gui-angular';

    @Component({
      imports: [CommonModule, Angular.FormComponent],
      selector: 'app-my-form',
      template: `<gui-form [config]="config"></gui-form>`,
    })
    export class MyForm {
      protected config = {
        formDef: [
          gui.inputs.booleanInput('hasDiscountCode', { label: 'I have a discount code' }),
          gui.inputs.numberInput('driverAge', { label: 'Driver age' }),
          gui.inputs.textInput('discountCode', {
            label: 'Discount code',
            include: { in: ['hasDiscount'] },
          }),
        ],
        data: {},
        formConfig: {
          widgetLoaders: Angular.widgetLoaders,
          states: {
            hasDiscount: '$form.hasDiscountCode === true',
            isAdult: '$form.driverAge >= 18',
          },
        },
      };
    }
    ```

  </Fragment>
  <Fragment slot="lit">
    ```ts
    import { LitElement, html } from 'lit';
    import { customElement } from 'lit/decorators.js';
    import { gui } from '@golemui/gui-shared';
    import * as Lit from '@golemui/gui-lit';

    @customElement('my-form')
    export class MyForm extends LitElement {
      config = {
        formDef: [
          gui.inputs.booleanInput('hasDiscountCode', { label: 'I have a discount code' }),
          gui.inputs.numberInput('driverAge', { label: 'Driver age' }),
          gui.inputs.textInput('discountCode', {
            label: 'Discount code',
            include: { in: ['hasDiscount'] },
          }),
        ],
        data: {},
        formConfig: {
          widgetLoaders: Lit.widgetLoaders,
          states: {
            hasDiscount: '$form.hasDiscountCode === true',
            isAdult: '$form.driverAge >= 18',
          },
        },
      };

      override createRenderRoot() { return this; }

      override render() {
        return html`<gui-form .config=${this.config}></gui-form>`;
      }
    }
    ```

  </Fragment>
  <Fragment slot="vue">
    ```vue
    <script setup lang="ts">
    import { gui } from '@golemui/gui-shared';
    import { GuiForm, widgetLoaders } from '@golemui/gui-vue';

    const formDef = [
      gui.inputs.booleanInput('hasDiscountCode', { label: 'I have a discount code' }),
      gui.inputs.numberInput('driverAge', { label: 'Driver age' }),
      gui.inputs.textInput('discountCode', {
        label: 'Discount code',
        include: { in: ['hasDiscount'] },
      }),
    ];

    const formConfig = {
      widgetLoaders,
      states: {
        hasDiscount: '$form.hasDiscountCode === true',
        isAdult: '$form.driverAge >= 18',
      },
    };

    const config = { formDef, data: {}, formConfig };
    </script>

    <template>
      <GuiForm :config="config" />
    </template>
    ```

  </Fragment>
</IntegrationTabs>

  </Fragment>
  <Fragment slot="json">

In the JSON path, states are declared **inside the form payload itself** under the top-level `"states"` key. There is no `formConfig` wrapper — the engine reads `"states"` directly off the parsed JSON.

```json
{
  "states": {
    "hasDiscount": "$form.hasDiscountCode === true",
    "isAdult": "$form.driverAge >= 18"
  },
  "form": [
    {
      "kind": "input",
      "type": "checkbox",
      "path": "hasDiscountCode",
      "label": "I have a discount code"
    },
    {
      "kind": "input",
      "type": "number",
      "path": "driverAge",
      "label": "Driver age"
    },
    {
      "kind": "input",
      "type": "textinput",
      "path": "discountCode",
      "label": "Discount code",
      "include": { "in": ["hasDiscount"] }
    }
  ]
}
```

Wire the parsed JSON into your form component the same way as any other JSON `formDef` — no extra prop is needed for states:

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import * as React from '@golemui/gui-react';
    import formDef from './my-form.json';

    const config = { formDef, data: {} };

    export function MyForm() {
      return <React.GuiForm config={config} />;
    }
    ```

  </Fragment>
  <Fragment slot="angular">
    ```ts
    import { Component } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import * as Angular from '@golemui/gui-angular';
    import formDef from './my-form.json';

    @Component({
      imports: [CommonModule, Angular.FormComponent],
      selector: 'app-my-form',
      template: `<gui-form [config]="config"></gui-form>`,
    })
    export class MyForm {
      protected config = {
        formDef,
        data: {},
        customWidgetLoaders: Angular.widgetLoaders,
      };
    }
    ```

  </Fragment>
  <Fragment slot="lit">
    ```ts
    import { LitElement, html } from 'lit';
    import { customElement } from 'lit/decorators.js';
    import * as Lit from '@golemui/gui-lit';
    import formDef from './my-form.json';

    @customElement('my-form')
    export class MyForm extends LitElement {
      config = {
        formDef,
        data: {},
        customWidgetLoaders: Lit.widgetLoaders,
      };

      override createRenderRoot() { return this; }

      override render() {
        return html`<gui-form .config=${this.config}></gui-form>`;
      }
    }
    ```

  </Fragment>
  <Fragment slot="vue">
    ```vue
    <script setup lang="ts">
    import { GuiForm, widgetLoaders } from '@golemui/gui-vue';
    import formDef from './my-form.json';

    const config = {
      formDef,
      data: {},
      customWidgetLoaders: widgetLoaders,
    };
    </script>

    <template>
      <GuiForm :config="config" />
    </template>
    ```

  </Fragment>
</IntegrationTabs>

  </Fragment>
</DslTabs>

## What you can do with a state name

Once a state is named, you reference it in three places:

1. **`include` / `exclude`** — gate whether a widget renders.
2. **Per-state prop overrides** — change a widget's label, disabled flag, items list, etc. when the state is active.
3. **Inline `when`** — a one-shot expression that doesn't get a name.

See the sub-pages for each pattern.

## In this section

- **[Include & Exclude](/features/states/include-exclude/)** — gate which widgets render.
- **[Properties](/features/states/properties/)** — override widget props per state.
- **[Inline `when`](/features/states/inline-when/)** — ad-hoc conditionals without naming a state.
- **[Composing states](/features/states/composing/)** — combine multiple state expressions.

<Aside>
States are also a first-class part of the **Form Definition API** — see [States in the form definition](/form-definition/states/) for the full programmatic reference.
</Aside>
