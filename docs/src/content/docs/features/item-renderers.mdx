---
title: Item Renderers
description: Provide custom item templates for dropdowns, lists, and other choice widgets.
sidebar:
  order: 2
---

import DslTabs from '../../../components/DslTabs.astro';
import IntegrationTabs from '../../../components/IntegrationTabs.astro';

Some widgets render a list of items — `dropdown`, `list`, `radiogroup`, and similar. By default, each item is rendered as a label/value pair. **Item renderers** let you supply a richer template (e.g. a country flag next to its label, a product card with a thumbnail and a price).

An item renderer is a small framework-specific function or component that receives the current item and returns the template for it. The renderer is registered under a name; widgets reference that name through their `itemRenderer` prop.

The shape of `CountryItemRenderer` itself is framework-specific — see [Extending GolemUI / Item Renderers](/extending/item-renderers/) for the implementation contract. The wiring below shows how to register and use it once it exists.

## Wiring an item renderer

<DslTabs>
  <Fragment slot="dx">

In the Programmatic API, item renderers go inside `formConfig.itemRenderers`. The widget references the renderer by name through its `itemRenderer` prop.

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import { gui } from '@golemui/gui-shared';
    import * as React from '@golemui/gui-react';
    import { CountryItemRenderer } from './CountryItemRenderer';

    const formDef = [
      gui.inputs.dropdown('country', {
        items: [
          { id: 'US', flag: '🇺🇸', label: 'United States' },
          { id: 'CA', flag: '🇨🇦', label: 'Canada' },
        ],
        labelField: 'label',
        valueField: 'id',
        itemRenderer: 'countryItemRenderer',
      }),
    ];

    const formConfig = {
      widgetLoaders: React.widgetLoaders,
      itemRenderers: {
        countryItemRenderer: CountryItemRenderer,
      },
    };

    export function MyForm() {
      return (
        <GuiForm
          formDef={formDef}
          data={{}}
          formConfig={formConfig}
        />
      );
    }
    ```

  </Fragment>
  <Fragment slot="angular">
    ```ts
    import { Component } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { gui } from '@golemui/gui-shared';
    import * as Angular from '@golemui/gui-angular';
    import { CountryItemRenderer } from './country-item-renderer.component';

    @Component({
      imports: [CommonModule, Angular.FormComponent],
      selector: 'app-my-form',
      template: `<gui-form
        [formDef]="formDef"
        [data]="formData"
        [formConfig]="formConfig"
      ></gui-form>`,
    })
    export class MyForm {
      protected formDef = [
        gui.inputs.dropdown('country', {
          items: [
            { id: 'US', flag: '🇺🇸', label: 'United States' },
            { id: 'CA', flag: '🇨🇦', label: 'Canada' },
          ],
          labelField: 'label',
          valueField: 'id',
          itemRenderer: 'countryItemRenderer',
        }),
      ];
      protected formData = {};
      protected formConfig = {
        widgetLoaders: Angular.widgetLoaders,
        itemRenderers: {
          countryItemRenderer: CountryItemRenderer,
        },
      };
    }
    ```

  </Fragment>
  <Fragment slot="lit">
    ```ts
    import { LitElement, html } from 'lit';
    import { customElement } from 'lit/decorators.js';
    import { gui } from '@golemui/gui-shared';
    import * as Lit from '@golemui/gui-lit';
    import { countryItemRenderer } from './country-item-renderer';

    @customElement('my-form')
    export class MyForm extends LitElement {
      formDef = [
        gui.inputs.dropdown('country', {
          items: [
            { id: 'US', flag: '🇺🇸', label: 'United States' },
            { id: 'CA', flag: '🇨🇦', label: 'Canada' },
          ],
          labelField: 'label',
          valueField: 'id',
          itemRenderer: 'countryItemRenderer',
        }),
      ];
      formData = {};
      formConfig = {
        widgetLoaders: Lit.widgetLoaders,
        itemRenderers: { countryItemRenderer },
      };

      override createRenderRoot() { return this; }

      override render() {
        return html`
          <gui-form
            .formDef=${this.formDef}
            .data=${this.formData}
            .formConfig=${this.formConfig}
          ></gui-form>
        `;
      }
    }
    ```

  </Fragment>
</IntegrationTabs>

  </Fragment>
  <Fragment slot="json">

In the JSON path, item renderers go into the **standalone `itemRenderers` prop** on the form component. The JSON references the renderer by name through the dropdown's `"itemRenderer"` field.

```json
{
  "form": [
    {
      "kind": "input",
      "type": "dropdown",
      "path": "country",
      "items": [
        { "id": "US", "flag": "🇺🇸", "label": "United States" },
        { "id": "CA", "flag": "🇨🇦", "label": "Canada" }
      ],
      "labelField": "label",
      "valueField": "id",
      "itemRenderer": "countryItemRenderer"
    }
  ]
}
```

<IntegrationTabs>
  <Fragment slot="react">
    ```tsx
    import * as React from '@golemui/gui-react';
    import formDef from './my-form.json';
    import { CountryItemRenderer } from './CountryItemRenderer';

    export function MyForm() {
      return (
        <GuiForm
          formDef={formDef}
          data={{}}
          customWidgetLoaders={React.widgetLoaders}
          itemRenderers={{ countryItemRenderer: CountryItemRenderer }}
        />
      );
    }
    ```

  </Fragment>
  <Fragment slot="angular">
    ```ts
    import { Component } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import * as Angular from '@golemui/gui-angular';
    import { CountryItemRenderer } from './country-item-renderer.component';
    import formDef from './my-form.json';

    @Component({
      imports: [CommonModule, Angular.FormComponent],
      selector: 'app-my-form',
      template: `<gui-form
        [formDef]="formDef"
        [data]="formData"
        [customWidgetLoaders]="widgetLoaders"
        [itemRenderers]="itemRenderers"
      ></gui-form>`,
    })
    export class MyForm {
      protected formDef = formDef;
      protected formData = {};
      protected widgetLoaders = Angular.widgetLoaders;
      protected itemRenderers = { countryItemRenderer: CountryItemRenderer };
    }
    ```

  </Fragment>
  <Fragment slot="lit">
    ```ts
    import { LitElement, html } from 'lit';
    import { customElement } from 'lit/decorators.js';
    import * as Lit from '@golemui/gui-lit';
    import { countryItemRenderer } from './country-item-renderer';
    import formDef from './my-form.json';

    @customElement('my-form')
    export class MyForm extends LitElement {
      formDef = formDef;
      formData = {};
      widgetLoaders = Lit.widgetLoaders;
      itemRenderers = { countryItemRenderer };

      override createRenderRoot() { return this; }

      override render() {
        return html`
          <gui-form
            .formDef=${this.formDef}
            .data=${this.formData}
            .customWidgetLoaders=${this.widgetLoaders}
            .itemRenderers=${this.itemRenderers}
          ></gui-form>
        `;
      }
    }
    ```

  </Fragment>
</IntegrationTabs>

  </Fragment>
</DslTabs>

## See also

- [Extending GolemUI / Item Renderers](/extending/item-renderers/) — implementing renderers per framework.
- [Widgets Reference / Dropdown](/widgets-reference/input-fields/dropdown/) — every prop a dropdown supports.
